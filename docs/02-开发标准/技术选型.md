# 技术选型（一期锁定）

| 能力 | 选型 | 备注 |
|------|------|------|
| 语言 | Python ≥ 3.12 | |
| 包管理 | uv + pyproject.toml | |
| Agent | LangChain 1.x `create_agent` | 单 Agent + tools |
| RAG 切分 | RecursiveCharacterTextSplitter | 与教程同思路 |
| Embedding | SiliconFlow `BAAI/bge-m3` | 1024 维；不用 Pro/ |
| 向量库 | Milvus Standalone | `localhost:19530`，COSINE |
| 对话模型 | DeepSeek（OpenAI 兼容协议） | `BASE_URL` + `MODEL` |
| 天气 | Open-Meteo + httpx | 本地 Tool，无 Key |
| 外部工具 | MCP（FastMCP）stdio | 新闻 / 舆情 / 邮件 |
| 配置 | python-dotenv | `.env` 不进 Git |

## 一期明确不做

- LangGraph 多 Agent（二期）
- Rerank / Hybrid Search
- 业务服务 Docker 化（Milvus 仍用 Docker）
- 前端、鉴权中台

## 更换选型

必须先改 [决策记录](../04-决策记录.md)，再改代码与本文。
