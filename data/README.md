# data — 结构化演示数据

| 文件 | 说明 |
|------|------|
| `demo_db.py` | SQLite 初始化与查询 |
| `demo_biz.db` | 运行后生成（gitignore） |

```bash
uv run python -m data.demo_db
```

表：`orders` / `tickets` / `accounts`（虚构演示数据，非真实客户数据）。
